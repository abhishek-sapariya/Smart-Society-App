<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    app:srcCompat="@drawable/gradient"
    tools:context=".ResidentComplaintsFragment">

    <Button
        android:id="@+id/upload_complaint_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Upload Complaint"
        android:background="@drawable/custom_button"
        />

    <Button
        android:id="@+id/show_complaint_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Complaints"
        android:layout_marginTop="10dp"
        android:background="@drawable/custom_button"
        />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/complaint_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="10dp"
        android:layout_weight="0.8"

        />

    <!--<Button
        android:id="@+id/delete_complaint_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="Delete Complaint"
        android:layout_marginBottom="10dp"
        android:background="@drawable/custom_button"
        />
-->

</LinearLayout>